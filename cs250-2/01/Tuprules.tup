.gitignore

CFLAGS += -Wall
CFLAGS += -Wextra
CFLAGS += -std=c++0x
CFLAGS += -pedantic
CFLAGS += -g
CFLAGS += -m32

LINKFLAGS += -m32
LINKFLAGS += -mwindows

!cxx = |> wineg++ $(CFLAGS) -c %f -o %o |> %B.o
!link = |> wineg++ $(LINKFLAGS) %f -o %o |>

